name: "Run Unit Tests"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:

  run:
    runs-on: ubuntu:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run Tests
        run: npm run test:unit
